{
  "name": "game-olympia-admin",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check auth.js && node --check tables.js && node --check rules.js && node --check realtime.js && node --check excel.js && node --check migrations.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "ejs": "^3.1.10",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.0",
    "socket.io": "^4.8.1"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
